<footer class="background-gray text-white py-10">
  <div class="max-w-7xl mx-auto px-4">
      
      <div class="flex justify-center">
        <ul class="flex justify-evenly w-[75%]">
          <li><a href="/profile" class="hover:underline text-gray-400 hover:text-white hover:bg-gray-800 rounded-xl p-4 ">Your Account</a></li>
          <% if (!user) { %>
            <li><a href="/login" class="hover:underline text-gray-400 hover:text-white hover:bg-gray-800 rounded-xl p-4">Log in</a></li>
          <% } %>
          <li><a href="/cart" class="hover:underline text-gray-400 hover:text-white hover:bg-gray-800 rounded-xl p-4">Cart</a></li>
          <li><a href="/wishlist" class="hover:underline text-gray-400 hover:text-white hover:bg-gray-800 rounded-xl p-4">Wishlist</a></li>
          <li><a href="/shop" class="hover:underline text-gray-400 hover:text-white hover:bg-gray-800 rounded-xl p-4">Shop</a></li>
        </ul>
      </div>
     

      
  
    <div
      class="border-t border-gray-700 mt-8 pt-8 text-center text-sm text-gray-400"
    >
      <p>© 2026 NovaMart.com. All rights reserved.</p>
    </div>
  </div>
</footer>
<script>
  feather.replace();

  document.getElementById("cartHeadItemCount").innerText =
    document.getElementById("itemsWithStock").value;
  document.getElementById("cartItemQty").innerText =
    document.getElementById("itemsWithStock").value;

  const accountBtn = document.getElementById("accountBtn");
  const accountDropdown = document.getElementById("accountDropdown");

  const cartBtn = document.getElementById("cartBtn");
  const cartDropdown = document.getElementById("cartDropdown");

  accountBtn.addEventListener("click", () => {
    accountDropdown.classList.toggle("hidden");
  });

  // Optional: hide dropdown if clicked outside
  window.addEventListener("click", (e) => {
    if (!accountBtn.contains(e.target) && !accountDropdown.contains(e.target)) {
      accountDropdown.classList.add("hidden");
    }
  });

  cartBtn.addEventListener("click", () => {
    cartDropdown.classList.toggle("hidden");
  });

  // Optional: hide dropdown if clicked outside
  window.addEventListener("click", (e) => {
    if (!cartBtn.contains(e.target) && !cartDropdown.contains(e.target)) {
      cartDropdown.classList.add("hidden");
    }
  });
</script>
