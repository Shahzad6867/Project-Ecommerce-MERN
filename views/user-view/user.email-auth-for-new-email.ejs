<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/novamart.png" type="image/icon-x" />
    <title>Novamart - Update Email</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>
    <script src="https://unpkg.com/feather-icons"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/izitoast/dist/css/iziToast.min.css"/>
    <link rel="stylesheet" href="/styles/style2.css">
    <script src="https://cdn.jsdelivr.net/npm/izitoast/dist/js/iziToast.min.js"></script>
   
</head>
<body class="flex items-center justify-center p-4">
    <div class="card shadow-2xl p-8 max-w-md w-full transform transition-all duration-500 hover:scale-[1.01]">
        <div class="text-center mb-8">
            <div class="w-20 h-20 bg-yellow-100 rounded-full flex items-center justify-center mx-auto mb-4">
                <i data-feather="key" class="w-10 h-10 text-yellow-500"></i>
            </div>
            <h1 class="text-3xl font-bold text-gray-800 mb-2">Update Email</h1>
            <p class="text-gray-600">Enter the email address you want to change :

            </p>
        </div>

        <form id="resetPassEmailForm" method="post" action="/email-auth-for-new-email" class="space-y-6"  novalidate>
            <input type="hidden" id="serverMessage"  value="<%= typeof message !== 'undefined' ? message : '' %>">
            <div class="relative">
                <label for="oldEmail" class="font-bold text-gray-700">Current Email Address</label>
                    <input id="oldEmail"  type="email"  value="<%= userEmail %>" placeholder="Enter your email" class="input-field  mt-1 text-gray-700 border-gray-300" disabled>
            </div>
            <div class="relative">
                <label for="email" class="text-gray-700 font-bold">New Email Address</label>
                <input id="email" type="email" name="email" value=" " placeholder="Enter your email" class="input-field mt-1 " >
            </div>

            <button type="submit" class="w-full py-3 px-4 btn-primary text-gray-700 font-semibold rounded-lg transition duration-300 flex items-center justify-center space-x-2">
                <span>Proceed</span>
            </button>
        </form>

        
            <div class="mt-6 text-center">
                <a href="/profile" class="text-gray-700 text-md font-medium hover:text-green-500 transition duration-300 flex items-center justify-center space-x-1">
                    <i data-feather="arrow-left" class="w-4 h-4"></i>
                    <span>Back to Profile</span>
                </a>
            </div>
       
    </div>

    <script src="/javascript/user-javascript/emailAuth.js">
       
    </script>
</body>
</html>
